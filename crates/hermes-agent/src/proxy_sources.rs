//! Egress proxy integrations (P2).
//!
//! PARITY: `agent/proxy_sources/__init__.py` @ 5d59366 (whole module):
//! "Egress proxy integrations — see `agent.proxy_sources.iron_proxy`
//! (ironsh/iron-proxy wrapper)." The `iron_proxy` backend itself is a
//! separate (missing) row; this root only declares the package surface.
