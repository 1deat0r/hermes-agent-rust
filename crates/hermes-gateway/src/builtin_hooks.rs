//! Built-in gateway hooks that are always registered (P4).
//!
//! PARITY: `gateway/builtin_hooks/__init__.py` @ 5d59366 (whole module):
//! "Built-in gateway hooks that are always registered." The package holds
//! no hook implementations at this pin — only the always-registered
//! contract, pinned by the surface test.
