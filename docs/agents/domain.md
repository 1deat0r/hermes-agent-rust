# Domain Docs (Hermes-Agent-Rust)

Single-context repo. Before exploring, read:

- `PLAN.md` §1–§5 (what Hermes is, fidelity model, crate architecture, phases, parity matrix) — the domain vocabulary source.
- `docs/long-horizon-100.md` (phase order, skills doctrine, cadence).
- `docs/board-review-r1.md` (adjudicated decisions — binding precedent).
- `tools/port_status.json` `retarget_note` fields (per-module seam/gap notes).

No `CONTEXT.md`/ADRs exist; per the skill, proceed silently — `PLAN.md` is the glossary. Use upstream module names verbatim (`agent/portal_tags.py`, not paraphrases). If output contradicts a board ruling, surface it explicitly rather than silently overriding.
